implemented api's:

1.get():
shows all the objects present in the database.

2.get(id):
gives you specific object whose id is the given id.

3.post() :
Allows you to post a new element in the database collection.

4.put(id):
Allows you to update the existing element in the collection.

5.delete(id):
Allows you delete the element with the given id.
